{
  "name": "iknowyourbirthday",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npx serve public",
    "dump": "node db.js",
    "surge": "npx surge --domain https://iknowyourbirthday.surge.sh public",
    "deploy": "npm run dump &&  npm run surge"
  },
  "dependencies": {
    "@prisma/client": "^4.5.0"
  },
  "devDependencies": {
    "prisma": "^4.5.0"
  }
}
